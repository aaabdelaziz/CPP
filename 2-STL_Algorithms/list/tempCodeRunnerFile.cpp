
  // for (const auto& it : mylist)
  //   cout << it << " - ";

  // cout << endl;

  // mylist.push_back(7);

  // for (const auto& it : mylist)
  //   cout << it << " - ";

  // cout << endl;

  // mylist.push_front(1);
  // for (const auto& it : mylist)
  //   cout << it << " - ";

  // cout << endl;

  // // To insert at second index of the lisr
  // list<int>::iterator itr = find(mylist.begin(), mylist.end(), 4);
  // mylist.insert(itr, 30);

  // for (const auto& it : mylist)
  //   cout << it << " - ";